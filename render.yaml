services:
  - type: web
    runtime: node
    name: detailerz
    buildCommand: cd app && npm ci && npm --prefix web ci && npm run build
    startCommand: cd app && npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
