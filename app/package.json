{
  "name": "detailerz-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "setup": "npm install --prefix web",
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web ci --include=dev && npm --prefix web run build",
    "start": "node server.js",
    "lint": "npm --prefix web run lint"
  },
  "dependencies": {
    "nodemailer": "^7.0.6"
  }
}